# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将模块与文档固定到 Calcit 0.15.3。
- 使用 `surface-latest-v2` 检查完整 Snapshot；当前没有需要应用的语法改写。
- 保持 C-safe async Stream、watcher 取消和背压契约不变。

## English

- Pin the module and documentation to Calcit 0.15.3.
- Check the full Snapshot with `surface-latest-v2`; no syntax rewrite is currently required.
- Preserve the C-safe async Stream, watcher cancellation, and backpressure contracts.

## 验证 / Verification

- `caps --strict --ci`
- Calcit format, strict type, quality, dynamic-method, documentation, and fix-preview checks
- Rust format, tests, strict Clippy, and release build
- C-safe export audit and real watcher smoke test
