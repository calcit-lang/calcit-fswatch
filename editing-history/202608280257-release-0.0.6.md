# Release 0.0.6 / 发布 0.0.6

- Releases the cancellable C-safe async Stream v1 filesystem watcher.
- 发布基于 C 安全 async Stream v1 的可取消文件监听器。
- Adds typed options/events, full schema coverage, explicit completion, and removes legacy Rust callback ABI exports.
- 增加类型化 options/events、完整 schema、显式完成，并移除旧 Rust callback ABI 导出。
